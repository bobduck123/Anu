# Presence Studio hosted owner proof

## Purpose

This evidence pack records the hosted owner-bound Studio V2 proof gate. It exists to separate truthful hosted owner evidence from local Room 11-shaped fixture evidence.

## Scope

This pass is read-only by default.

It may verify:

- hosted owner sign-in;
- owner-bound backend read access;
- hosted Studio V2 editor rendering;
- layout-composition controls in the hosted editor;
- hosted private preview rendering;
- anonymous public route behaviour for the configured slug;
- absence of publish or owner mutation requests during the proof.

It must not:

- publish;
- mutate public state;
- call mutation-capable lifecycle specs;
- expose secrets;
- claim GGM is public launch material;
- treat fallback/frontend visual copy as system-native migration proof.

## Redacted env/config checklist

Real values are kept outside git in `.env.hosted-owner-proof.local` or in the local shell process.

Required keys:

- `PRESENCE_HOSTED_SMOKE`
- `PRESENCE_E2E_BASE_URL`
- `PRESENCE_E2E_API_URL`
- `PRESENCE_E2E_OWNER_EMAIL`
- `PRESENCE_E2E_OWNER_PASSWORD`
- `PRESENCE_STUDIO_V2_HOSTED_PILOT_ROOM_ID`
- `PRESENCE_STUDIO_V2_HOSTED_PILOT_SLUG`

Optional keys:

- `PRESENCE_STUDIO_V2_HOSTED_LEGACY_SLUG`
- `PRESENCE_STUDIO_V2_HOSTED_LEGACY_ROOM_ID`
- `PRESENCE_HOSTED_DRAFT_WRITE_ENABLED`

For the read-only proof, `PRESENCE_HOSTED_DRAFT_WRITE_ENABLED` must be unset or `0`.

For the draft write/revert proof, `PRESENCE_HOSTED_DRAFT_WRITE_ENABLED` must be `1`.

## Expected commands

```powershell
npm.cmd run typecheck
npm.cmd run build
npm.cmd run test:e2e -- tests/e2e/presence-studio-v2-hosted-owner-readonly.spec.ts --project=chromium --retries=0 --workers=1
npm.cmd run test:e2e -- tests/e2e/presence-studio-v2-hosted-owner-draft-write.spec.ts --project=chromium --retries=0 --workers=1
```

## Evidence generated by the hosted smoke

- `route-status-matrix.json`
- `ROUTE_STATUS_MATRIX.md`
- `VALIDATION_RECORD.md`
- `01-hosted-layout-composition-controls.png`
- `02-hosted-private-preview-readonly.png`
- `03-public-canonical-route-readonly.png`
- `04-public-legacy-route-readonly.png`
- `DRAFT_WRITE_REVERT_SUMMARY.md`
- `draft-write-route-status-matrix.json`
- `05-draft-write-before-mutation.png`
- `06-draft-write-after-reload.png`
- `07-private-preview-after-draft-change.png`
- `08-draft-write-after-revert-reload.png`
- `09-public-canonical-route-after-draft-revert.png`
- `10-public-legacy-route-after-draft-revert.png`

Screenshots are generated only after the relevant proof condition passes. A failed hosted run should not leave private screenshots behind.

## Current run result

Passed as a hosted read-only proof.

Confirmed:

- hosted owner authentication reached Studio;
- owner-scoped backend list, detail, editor overview, and draft reads returned 200;
- hosted Studio V2 editor loaded for room `11`;
- layout-composition controls rendered in the hosted editor;
- private preview loaded with no editor instrumentation;
- anonymous `/p/ggm-christina-goddard` returned a truthful unavailable/private 404;
- anonymous `/presence/ggm-christina-goddard` returned a truthful unavailable/private 404;
- no publish request was triggered;
- no draft save/revert was attempted;
- no public state mutation was attempted.

The private preview page uses the existing safe preview-generation endpoint, `POST /api/presence/owner/rooms/11/editor/preview`. The frontend client marks this as `ownerReadFetch` with `safeEnsurePost`; it does not publish or overwrite owner content.

## Hosted draft write/reload/revert result

Passed.

Confirmed:

- hosted owner draft read succeeded;
- `PRESENCE_HOSTED_DRAFT_WRITE_ENABLED=1` was explicitly enabled;
- one layout-composition draft field was changed: `content_config.chambers[].composition.layoutId`;
- original value was captured as `gallery-wall`;
- temporary value was saved as `portal-threshold`;
- hosted owner Studio reload showed `portal-threshold`;
- private preview rendered the temporary `portal-threshold` layout;
- original draft payload was restored;
- hosted owner Studio reload showed `gallery-wall` again;
- anonymous `/p/ggm-christina-goddard` remained `404`;
- anonymous `/presence/ggm-christina-goddard` remained `404`;
- no publish action occurred.

## Known limits

- This proof does not publish.
- This proof does not prove migration completeness for GGM or BBB.
- This proof does not repair owner policy, control-plane credentials, or backend configuration.
- Public-route checks are route-render checks only; they do not intentionally call analytics-writing public-detail APIs directly.
