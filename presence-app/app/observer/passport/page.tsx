import { ObserverPassportClient } from "@/components/presence/graph/ObserverPassportClient";

export const metadata = {
  title: "Observer Passport | Presence",
  description: "A memory of Rooms, Field Notes, Mood Boards, and Paths.",
};

export default function ObserverPassportPage() {
  return <ObserverPassportClient />;
}

