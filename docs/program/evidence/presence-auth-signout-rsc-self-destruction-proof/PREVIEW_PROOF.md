# Preview Proof

## Automated Assertions

With the owner still signed in after editor navigation and hard refresh:

- `/studio/101/editor/preview` renders `Draft preview not public`;
- no `/auth/sign-out` request is observed while entering preview;
- the session marker remains present.

The established preview tests additionally verify:

- owner preview renders draft state;
- anonymous preview is denied without draft leakage;
- non-owner preview is denied;
- preview remains private/noindex.

## Hosted Check

After deployment, Kimi must confirm the standard hosted preview route renders for the owner without any `Set-Cookie` expiry generated by sign-out navigation.
